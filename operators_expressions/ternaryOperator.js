// Ternary Operators

let a = 10;
let b = 20;

let greaterNum = a > b ? 'a is greater' : 'b is greater';
console.log(greaterNum); // b is greater

let smallerNum = a < b ? 'a is smaller' : 'b is smaller';
console.log(smallerNum); // a is smaller